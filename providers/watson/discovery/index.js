const collections = require('./api/collections');
const configurations = require('./api/configurations');
const documents = require('./api/documents');
const environments = require('./api/environments');

module.exports = {
  collections,
  configurations,
  documents,
  environments,
};
